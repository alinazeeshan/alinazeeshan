name: Update GitHub Contribution Count

on:
  schedule:
    - cron: "17 0 * * *"

  workflow_dispatch:

permissions:
  contents: write

jobs:
  update:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Get GitHub contribution total
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |

          RESPONSE=$(curl -s \
            -H "Authorization: bearer $GH_TOKEN" \
            -H "Content-Type: application/json" \
            -d '{"query":"query { user(login: \"alinazeeshan\") { contributionsCollection { contributionCalendar { totalContributions } } } }"}' \
            https://api.github.com/graphql)

          CONTRIBUTIONS=$(echo "$RESPONSE" | jq -r \
            '.data.user.contributionsCollection.contributionCalendar.totalContributions')

          echo "Total contributions: $CONTRIBUTIONS"

          if [ "$CONTRIBUTIONS" = "null" ] || [ -z "$CONTRIBUTIONS" ]; then
            echo "Could not get contribution count."
            echo "$RESPONSE"
            exit 1
          fi

          sed -i \
            "s/<!-- CONTRIBUTIONS_START -->.*<!-- CONTRIBUTIONS_END -->/<!-- CONTRIBUTIONS_START -->$CONTRIBUTIONS<!-- CONTRIBUTIONS_END -->/" \
            README.md

      - name: Save updated README
        run: |

          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

          git add README.md

          if git diff --cached --quiet; then
            echo "Contribution count has not changed."
          else
            git commit -m "Update GitHub contribution count"
            git push
          fi
